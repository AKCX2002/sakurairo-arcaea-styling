diff --git a/comments.php b/comments.php
index 3316959..f0aa1e2 100644
--- a/comments.php
+++ b/comments.php
@@ -183,14 +183,15 @@ function get_smilies_panel() {
                                             <label class="input-label">' . esc_html(iro_opt('comment_placeholder_text')) . '</label>
                                         </div>' . $smilies_box . 
                                         '<div id="upload-img-show"></div>',
-                'submit_button'     => '<div class="form-submit">
+                // WordPress formats submit_button with sprintf; URLs and labels are literal text.
+                'submit_button'     => str_replace('%', '%%', '<div class="form-submit">
                                             <input name="submit" type="submit" id="submit" class="submit" value=" ' . esc_attr(iro_opt('comment_submit_button_text')) . ' ">' . $smilies_button . $img_upload .'
                                             <label class="markdown-toggle">
                                                 <input type="checkbox" id="enable_markdown" name="enable_markdown">
                                                 <i class="fa-brands fa-markdown fa-sm"></i>
                                             </label>
                                             ' . wp_nonce_field('sakurairo_ajax_comment', 'sakurairo_comment_nonce', true, false) . '
-                                        </div>',
+                                        </div>'),
                 'comment_notes_after'  => '',
                 'comment_notes_before' => '',
                 'fields'            => (!is_user_logged_in()?apply_filters('comment_form_default_fields', array(
diff --git a/css/responsive.css b/css/responsive.css
index 5dfdef7..7815e84 100644
--- a/css/responsive.css
+++ b/css/responsive.css
@@ -937,4 +937,18 @@
     transform: scale(0.6);
     margin-block-end: -10px;
   }
-}
\ No newline at end of file
+}
+/* Native menu button retains the existing navigation appearance. */
+button.mo-nav-button {
+  border: 0;
+  border-radius: 0;
+  padding: 0;
+  background: transparent;
+  color: inherit;
+  box-shadow: none;
+  line-height: inherit;
+}
+button.mo-nav-button:focus-visible {
+  outline: 2px solid currentColor;
+  outline-offset: -3px;
+}
diff --git a/functions.php b/functions.php
index 7fdd4d4..4083178 100644
--- a/functions.php
+++ b/functions.php
@@ -504,6 +504,16 @@ function sakura_scripts()
     global $shared_lib_basepath;
 
     // 预加载主要样式文件
+    // Local theme edits must invalidate both the combined and separate stylesheet URLs.
+    $style_version = IRO_VERSION;
+    if (iro_opt('core_library_basepath')) {
+        $style_files = array_merge(
+            array(get_template_directory() . '/style.css'),
+            glob(get_template_directory() . '/css/*.{css,php}', GLOB_BRACE),
+            glob(get_template_directory() . '/css/content-style/*.css')
+        );
+        $style_version .= '.' . max(array_map('filemtime', $style_files));
+    }
     if(iro_opt('dev_mode',false) == false) { // 压缩并缓存主题样式
         
         function add_cache_control_header() { // 添加缓存策略
@@ -520,7 +530,7 @@ function sakura_scripts()
         if (strpos(get_option('permalink_structure'), 'index.php') !== false) {
             $index = 'index.php';
         }
-        $iro_css = $core_lib_basepath . '/css/' . $index . '?' . $sakura_header . '&' . $content_style . '&' . $wave . '&minify&' . IRO_VERSION;
+        $iro_css = $core_lib_basepath . '/css/' . $index . '?' . $sakura_header . '&' . $content_style . '&' . $wave . '&minify&' . $style_version;
         add_action('wp_head', function() use ($iro_css) {
             echo '<link rel="preload" href="' .$iro_css. '" as="style" onload="this.onload=null;this.rel=\'stylesheet\'">';
             echo '<link rel="stylesheet" href="' . $iro_css . '">';
@@ -530,7 +540,7 @@ function sakura_scripts()
         wp_enqueue_style('iro-css', $core_lib_basepath . '/style.css', array(), IRO_VERSION);
         wp_enqueue_style('iro-codes', $core_lib_basepath . '/css/shortcodes.css', array(), IRO_VERSION);
         wp_enqueue_style('iro-dark', $core_lib_basepath . '/css/dark.css', array('iro-css'), IRO_VERSION);
-        wp_enqueue_style('iro-responsive', $core_lib_basepath . '/css/responsive.css', array('iro-css'), IRO_VERSION);
+        wp_enqueue_style('iro-responsive', $core_lib_basepath . '/css/responsive.css', array('iro-css'), $style_version);
         wp_enqueue_style('iro-animation', $core_lib_basepath . '/css/animation.css', array('iro-css'), IRO_VERSION);
         wp_enqueue_style('iro-templates', $core_lib_basepath . '/css/templates.css', array('iro-css'), IRO_VERSION);
 
diff --git a/header.php b/header.php
index 692c56d..b52ebf4 100644
--- a/header.php
+++ b/header.php
@@ -59,7 +59,7 @@ header('X-Frame-Options: SAMEORIGIN');
 <head>
     <meta name="theme-color"  content="<?php echo iro_opt('theme_skin'); ?>">
     <meta charset="<?php bloginfo('charset'); ?>">
-    <meta content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0" name="viewport">
+    <meta content="width=device-width, initial-scale=1.0" name="viewport">
 
     <!-- 优化资源加载 -->
     <meta http-equiv="x-dns-prefetch-control" content="on">
@@ -137,7 +137,7 @@ header('X-Frame-Options: SAMEORIGIN');
         });
     });
 	</script>
-    <script src="<?= $core_lib_basepath . '/js/nav.js' ?>" defer></script>
+    <script src="<?= esc_url(add_query_arg('ver', iro_opt('core_library_basepath') ? filemtime(get_template_directory() . '/js/nav.js') : IRO_VERSION, $core_lib_basepath . '/js/nav.js')) ?>" defer></script>
 </head>
 
 <body <?php body_class(); ?>>
@@ -159,10 +159,10 @@ header('X-Frame-Options: SAMEORIGIN');
     
     <header class="site-header no-select" role="banner">
         <?php //移动端结构开始 ?>
-        <div class="mo-nav-button">
-            <i class="fa-solid fa-bars"></i>
-        </div>
-        <div class="mobile-nav">
+        <button type="button" class="mo-nav-button" aria-label="打开导航菜单" aria-controls="mobile-navigation" aria-expanded="false">
+            <i class="fa-solid fa-bars" aria-hidden="true"></i>
+        </button>
+        <div class="mobile-nav" id="mobile-navigation" inert>
 
         <?php if($show_search) : ?>
         <div class="mo-menu-search">
diff --git a/js/nav.js b/js/nav.js
index ed56452..1caaa0e 100644
--- a/js/nav.js
+++ b/js/nav.js
@@ -1108,6 +1108,8 @@ document.addEventListener("DOMContentLoaded", () => {
     }
 
     function openMenu(panel, button) {
+        button.setAttribute("aria-expanded", "true");
+        panel.inert = false;
 
         if (isHeaderHover()) { //hover会产生背景动画，干扰相关功能，但是无需判断动画先后，直接呼出即可
             panel.classList.add("open");
@@ -1145,6 +1147,8 @@ document.addEventListener("DOMContentLoaded", () => {
     }
 
     function closeMenu(panel, button,CloseDirect = false) {
+        button.setAttribute("aria-expanded", "false");
+        panel.inert = true;
 
         if (CloseDirect){ //直接关闭，hover不需要考虑背景动画顺序
             panel.classList.remove("open");
@@ -1316,6 +1320,17 @@ document.addEventListener("DOMContentLoaded", () => {
         });
     });
 
+    document.addEventListener("keydown", function (event) {
+        if (event.key === "Escape" && moNavButton.getAttribute("aria-expanded") === "true") {
+            if (navTransitionHandler) {
+                moHeader.removeEventListener("transitionend", navTransitionHandler);
+                navTransitionHandler = null;
+            }
+            closeMenu(moNavMenu, moNavButton, true);
+            moNavButton.focus();
+        }
+    });
+
     // 点击选项关闭
     document.querySelectorAll(".mobile-nav a, .mo_toc_panel a").forEach(link => {
         link.addEventListener("click", () => {
@@ -1445,6 +1460,8 @@ document.addEventListener("DOMContentLoaded", () => {
                     moHeader.classList.remove("bg");
                     moNavMenu.classList.remove("open");
                     moNavButton.classList.remove("open");
+                    moNavButton.setAttribute("aria-expanded", "false");
+                    moNavMenu.inert = true;
                     moTocMenu.classList.remove("open");
                     moTocButton.classList.remove("open");
 
